package com.app.facebookclone.repos;

import com.app.facebookclone.entities.Media;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MediaRepo extends JpaRepository<Media, Long> {
}
